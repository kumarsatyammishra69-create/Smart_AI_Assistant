# Smart AI Assistant
A chatbot built with Python, Streamlit, and LangChain using a local LLM.

## How to run
1. Install dependencies: `pip install -r requirements.txt`
2. Start Ollama: `ollama run llama3`
3. Run app: `streamlit run app.py`